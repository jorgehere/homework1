---

- hosts: webserver
  become: yes

  tasks:
   - name: Nginx setup
     apt: pkg=nginx state=installed update_cache=true
   - name: start nginx
     service: name=nginx state=started
   - name: index.html copy
     copy: src=index.html.j2 dest=/usr/share/nginx/html/index.html mode=0644
     
