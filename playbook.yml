---

- hosts: webserver
  become: yes

  tasks:
   - name: Nginx setup
     apt: pkg=nginx state=present update_cache=true
   - name: coppy html
     copy: src=index.html dest=/Users/jorgemurillocarranza/Desktop
     become: yes
  # - name: start n
   #  service: name=nginx state=started
     
 # handlers:
  # - name: start nginx
  #   service: name=nginx state=started
     
