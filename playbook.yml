---

- hosts: webserver
  become: yes

  tasks:
   - name: Nginx setup
     apt: pkg=nginx state=present update_cache=true
   - name: start n
     service: name=nginx state=started
   - name: coppy html
     copy: src=/etc/ansible/static-site-src/index.html dest=/Users/jorgemurillocarranza/Desktop/static_site
     become: yes
     
 # handlers:
  # - name: start nginx
  #   service: name=nginx state=started
     
