---

- hosts: webserver
  become: yes

  tasks:
   - name: Nginx setup
     apt: pkg=nginx state=present update_cache=true
   - name: coppy html
     template: src=/etc/ansible/index.html dest=/usr/share/nginx/html/index.html
     
 # handlers:
  # - name: start nginx
  #   service: name=nginx state=started
     
